ingest:
  kafka:
    broker: kafka:9092
    brokerAddressFamily: v4
    socketKeepAliveEnable: true
    topicMetadataRefreshInterval: 10s

aggregation:
  clickhouse:
    address: clickhouse:9000

sink:
  minCommitCount: 1
  namespaceRefetch: 1s
  namespaceRefetchTimeout: 1s
  kafka:
    brokers: kafka:9092
    brokerAddressFamily: v4
    socketKeepAliveEnable: true
    topicMetadataRefreshInterval: 10s
  dedupe:
    enabled: true
    driver: redis
    config:
      address: redis:6379
      database: 0
      expiration: 768h # 32d

postgres:
  url: postgres://postgres:postgres@postgres:5432/postgres?sslmode=disable
  autoMigrate: migration # Runs migrations as part of the service startup, valid values are: ent, migration, false

credits:
  enabled: true
  enableCreditThenInvoice: true

meters:
  # Sample meter to count API requests
  - slug: api_requests_total        # Unique identifier for the meter
    description: API Requests
    eventType: request              # Filter events by type
    aggregation: COUNT              # Aggregation method: COUNT, SUM, etc.
    groupBy:
      method: $.method              # HTTP Method: GET, POST, etc.
      route: $.route                # Route: /products/:product_id

  # Sample meter to sum API request duration
  - slug: api_requests_duration
    description: API Request Duration
    eventType: request
    aggregation: SUM
    valueProperty: $.duration_ms     # JSONPath to parse usage value
    groupBy:
      method: $.method
      route: $.route

  # Sample meter to count LLM Token Usage
  - slug: tokens_total
    description: AI Token Usage
    eventType: prompt               # Filter events by type
    aggregation: SUM
    valueProperty: $.tokens         # JSONPath to parse usage value
    groupBy:
      model: $.model                # AI model used: gpt4-turbo, etc.
      type: $.type                  # Prompt type: input, output, system

portal:
  enabled: true
  tokenSecret: this-isnt-secure

svix:
   # This is just a random JWT based on docker-compose's JWT_SECRET, so it's not sensitive
  apiKey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpYXQiOjE3MjI5NzYyNzMsImV4cCI6MjAzODMzNjI3MywibmJmIjoxNzIyOTc2MjczLCJpc3MiOiJzdml4LXNlcnZlciIsInN1YiI6Im9yZ18yM3JiOFlkR3FNVDBxSXpwZ0d3ZFhmSGlyTXUifQ.B0OVUl3w9m7nKkSjIpPrxbfXL9IuVimy8LXcdIc3q7k
  serverURL: http://svix:8071
  debug: false
  
aiUsage:
  enabled: true
  signing:
    currentKeyId: "wkc-p1-key-001"
    currentSeed: "c8548eb734e07ee13a62f75cd42d800ae5c72b00b7191cd3e3d2e30edf579fa8"
  authorizationTtl: "5m"
  worker:
    leaseDuration: "30s"
    batchSize: 50
  settlement:
    defaultChargeId: "ai-usage-charge"
    defaultFeatureKey: "ai_usage"
    defaultCurrency: "CNY"
  rateEntries:
    # LLM tokens (1 credit per token for Phase 1 testing)
    - resourceCode: "llm_input_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "llm_output_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "llm_cache_read_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "llm_cache_write_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "llm_reasoning_tokens"
      creditsPerUnit: 1
      unitSize: 1
    # Embedding and rerank
    - resourceCode: "embedding_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "rerank_calls"
      creditsPerUnit: 10
      unitSize: 1
    # Multimodal
    - resourceCode: "vlm_input_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "vlm_output_tokens"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "vlm_images"
      creditsPerUnit: 100
      unitSize: 1
    # Speech
    - resourceCode: "asr_milliseconds"
      creditsPerUnit: 1
      unitSize: 1000
    # Platform resources
    - resourceCode: "rag_queries"
      creditsPerUnit: 1
      unitSize: 1
    - resourceCode: "doc_parse_pages"
      creditsPerUnit: 10
      unitSize: 1
    - resourceCode: "mcp_tool_calls"
      creditsPerUnit: 5
      unitSize: 1
    - resourceCode: "web_searches"
      creditsPerUnit: 10
      unitSize: 1
    - resourceCode: "agent_runs"
      creditsPerUnit: 50
      unitSize: 1
