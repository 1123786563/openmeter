// Package service contains the synchronous authorization lifecycle. Database
// wiring is intentionally kept at the adapter boundary so its pure funding,
// evidence, and expiry decisions remain independently testable.
package service
