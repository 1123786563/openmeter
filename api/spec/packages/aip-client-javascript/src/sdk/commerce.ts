// Code generated by @openmeter/typespec-typescript. DO NOT EDIT.

import { type Client } from '../core.js'
import { unwrap, type RequestOptions } from '../lib/types.js'
import { paginateCursor } from '../lib/paginate.js'
import {
  getCustomerWallet,
  listRechargeProducts,
  createOrder,
  getOrder,
  createCheckoutSession,
  getCheckoutSession,
  createRefund,
  getRefund,
  wechatPaymentCallback,
  wechatRefundCallback,
  alipayPaymentCallback,
  listReceivablePeriods,
  createOfflinePayment,
  updateExternalInvoice,
} from '../funcs/commerce.js'
import type {
  GetCustomerWalletRequest,
  GetCustomerWalletResponse,
  ListRechargeProductsRequest,
  ListRechargeProductsResponse,
  CreateOrderRequest,
  CreateOrderResponse,
  GetOrderRequest,
  GetOrderResponse,
  CreateCheckoutSessionRequest,
  CreateCheckoutSessionResponse,
  GetCheckoutSessionRequest,
  GetCheckoutSessionResponse,
  CreateRefundRequest,
  CreateRefundResponse,
  GetRefundRequest,
  GetRefundResponse,
  WechatPaymentCallbackRequest,
  WechatPaymentCallbackResponse,
  WechatRefundCallbackRequest,
  WechatRefundCallbackResponse,
  AlipayPaymentCallbackRequest,
  AlipayPaymentCallbackResponse,
  ListReceivablePeriodsRequest,
  ListReceivablePeriodsResponse,
  CreateOfflinePaymentRequest,
  CreateOfflinePaymentResponse,
  UpdateExternalInvoiceRequest,
  UpdateExternalInvoiceResponse,
} from '../models/operations/commerce.js'
import type { CommerceReceivablePeriod } from '../models/types.js'

export class Commerce {
  constructor(private readonly _client: Client) {}

  /**
   * Get customer wallet
   *
   * Get a customer's Wallet view, including all credit buckets and recent
   * transactions.
   *
   * GET /customers/{customerId}/wallet
   */
  async getCustomerWallet(
    request: GetCustomerWalletRequest,
    options?: RequestOptions,
  ): Promise<GetCustomerWalletResponse> {
    return unwrap(await getCustomerWallet(this._client, request, options))
  }

  /**
   * List recharge products
   *
   * List all active recharge products available for purchase.
   *
   * GET /recharge-products
   */
  async listRechargeProducts(
    request?: ListRechargeProductsRequest,
    options?: RequestOptions,
  ): Promise<ListRechargeProductsResponse> {
    return unwrap(await listRechargeProducts(this._client, request, options))
  }

  /**
   * Create order
   *
   * Create a new order (plan purchase, subscription renewal, or wallet top-up).
   *
   * Returns HTTP 201 on first creation. Replaying the same idempotency key returns
   * the stored order with HTTP 200.
   *
   * POST /orders
   */
  async createOrder(
    request: CreateOrderRequest,
    options?: RequestOptions,
  ): Promise<CreateOrderResponse> {
    return unwrap(await createOrder(this._client, request, options))
  }

  /**
   * Get order
   *
   * Retrieve an order by its ID.
   *
   * GET /orders/{orderId}
   */
  async getOrder(
    request: GetOrderRequest,
    options?: RequestOptions,
  ): Promise<GetOrderResponse> {
    return unwrap(await getOrder(this._client, request, options))
  }

  /**
   * Create checkout session
   *
   * Create a checkout session for an order, initiating a payment attempt with the
   * specified provider.
   *
   * POST /orders/{orderId}/checkout-sessions
   */
  async createCheckoutSession(
    request: CreateCheckoutSessionRequest,
    options?: RequestOptions,
  ): Promise<CreateCheckoutSessionResponse> {
    return unwrap(await createCheckoutSession(this._client, request, options))
  }

  /**
   * Get checkout session
   *
   * Retrieve a checkout session by its ID (for polling payment status after QR code
   * expiry or page reload).
   *
   * GET /checkout-sessions/{sessionId}
   */
  async getCheckoutSession(
    request: GetCheckoutSessionRequest,
    options?: RequestOptions,
  ): Promise<GetCheckoutSessionResponse> {
    return unwrap(await getCheckoutSession(this._client, request, options))
  }

  /**
   * Create refund
   *
   * Create a refund request for an order.
   *
   * POST /refunds
   */
  async createRefund(
    request: CreateRefundRequest,
    options?: RequestOptions,
  ): Promise<CreateRefundResponse> {
    return unwrap(await createRefund(this._client, request, options))
  }

  /**
   * Get refund
   *
   * Retrieve a refund by its ID.
   *
   * GET /refunds/{refundId}
   */
  async getRefund(
    request: GetRefundRequest,
    options?: RequestOptions,
  ): Promise<GetRefundResponse> {
    return unwrap(await getRefund(this._client, request, options))
  }

  /**
   * WeChat Pay callback
   *
   * WeChat Pay payment callback. OpenMeter verifies the signature, confirms the
   * payment fact, and fulfills the order.
   *
   * The request body is the provider-signed WeChat Pay v3 notification: a JSON
   * envelope whose `resource` payload is AES-256-GCM encrypted. OpenMeter
   * verifies the RSA signature over the raw bytes, so the body is intentionally
   * not modeled with a schema and must be forwarded byte-for-byte.
   *
   * POST /payment-providers/wechat/callback
   */
  async wechatPaymentCallback(
    request: WechatPaymentCallbackRequest,
    options?: RequestOptions,
  ): Promise<WechatPaymentCallbackResponse> {
    return unwrap(await wechatPaymentCallback(this._client, request, options))
  }

  /**
   * WeChat Pay refund callback
   *
   * WeChat Pay refund callback. OpenMeter verifies and decrypts the notification,
   * then applies the authoritative refund fact.
   *
   * The request body is the provider-signed WeChat Pay v3 refund notification.
   * OpenMeter verifies the RSA signature over the raw bytes, so the body is
   * intentionally not modeled with a schema and must be forwarded
   * byte-for-byte.
   *
   * POST /payment-providers/wechat/refund-callback
   */
  async wechatRefundCallback(
    request: WechatRefundCallbackRequest,
    options?: RequestOptions,
  ): Promise<WechatRefundCallbackResponse> {
    return unwrap(await wechatRefundCallback(this._client, request, options))
  }

  /**
   * Alipay callback
   *
   * Alipay payment callback. OpenMeter verifies the signature, confirms the payment
   * fact, and fulfills the order.
   *
   * The request body is the `application/x-www-form-urlencoded` Alipay
   * notification whose field set varies by trade type. OpenMeter verifies the
   * RSA2 signature over the canonicalized raw form fields, so the body is
   * intentionally not modeled with a schema and must be forwarded
   * byte-for-byte.
   *
   * POST /payment-providers/alipay/callback
   */
  async alipayPaymentCallback(
    request: AlipayPaymentCallbackRequest,
    options?: RequestOptions,
  ): Promise<AlipayPaymentCallbackResponse> {
    return unwrap(await alipayPaymentCallback(this._client, request, options))
  }

  /**
   * List receivable periods
   *
   * List receivable periods for a customer.
   *
   * GET /customers/{customerId}/receivable-periods
   */
  async listReceivablePeriods(
    request: ListReceivablePeriodsRequest,
    options?: RequestOptions,
  ): Promise<ListReceivablePeriodsResponse> {
    return unwrap(await listReceivablePeriods(this._client, request, options))
  }

  /**
   * List receivable periods
   *
   * List receivable periods for a customer.
   *
   * Iterates every item across all pages, fetching more as the returned iterable is consumed.
   *
   * GET /customers/{customerId}/receivable-periods
   */
  listReceivablePeriodsAll(
    request: ListReceivablePeriodsRequest,
    options?: RequestOptions,
  ): AsyncIterable<CommerceReceivablePeriod> {
    return paginateCursor(
      (req, opts) => listReceivablePeriods(this._client, req, opts),
      request,
      options,
    )
  }

  /**
   * Create offline payment
   *
   * Record an offline payment (bank transfer, enterprise remittance) for a customer.
   * The payment is held for reconciliation before being applied to a receivable
   * period.
   *
   * POST /customers/{customerId}/offline-payments
   */
  async createOfflinePayment(
    request: CreateOfflinePaymentRequest,
    options?: RequestOptions,
  ): Promise<CreateOfflinePaymentResponse> {
    return unwrap(await createOfflinePayment(this._client, request, options))
  }

  /**
   * Update external invoice
   *
   * Attach or update an external invoice reference on a receivable period.
   *
   * PUT /customers/{customerId}/receivable-periods/{periodId}/external-invoice
   */
  async updateExternalInvoice(
    request: UpdateExternalInvoiceRequest,
    options?: RequestOptions,
  ): Promise<UpdateExternalInvoiceResponse> {
    return unwrap(await updateExternalInvoice(this._client, request, options))
  }
}
