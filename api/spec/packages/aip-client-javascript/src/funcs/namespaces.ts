// Code generated by @openmeter/typespec-typescript. DO NOT EDIT.

import { type Client, http } from '../core.js'
import { type Result, type RequestOptions } from '../lib/types.js'
import { request } from '../lib/request.js'
import { fromWire, assertValid } from '../lib/wire.js'
import * as schemas from '../models/schemas.js'
import type {
  ListNamespacesRequest,
  ListNamespacesResponse,
} from '../models/operations/namespaces.js'

/**
 * List namespaces
 *
 * List the namespaces available on this deployment. The default namespace is
 * always included in the response.
 *
 * GET /openmeter/namespaces
 */
export function listNamespaces(
  client: Client,
  req: ListNamespacesRequest,
  options?: RequestOptions,
): Promise<Result<ListNamespacesResponse>> {
  return request(() =>
    http(client)
      .get('openmeter/namespaces', options)
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.listNamespacesResponseWire, data)
        }
        return fromWire(data, schemas.listNamespacesResponse)
      }),
  )
}
