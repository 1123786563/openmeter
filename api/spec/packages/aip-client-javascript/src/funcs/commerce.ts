// Code generated by @openmeter/typespec-typescript. DO NOT EDIT.

import { type Client, http } from '../core.js'
import { type Result, type RequestOptions } from '../lib/types.js'
import { request } from '../lib/request.js'
import { toURLSearchParams } from '../lib/encodings.js'
import { toWire, toPathWire, fromWire, assertValid } from '../lib/wire.js'
import * as schemas from '../models/schemas.js'
import type {
  GetCustomerWalletRequest,
  GetCustomerWalletResponse,
  ListRechargeProductsRequest,
  ListRechargeProductsResponse,
  CreateOrderRequest,
  CreateOrderResponse,
  GetOrderRequest,
  GetOrderResponse,
  CreateCheckoutSessionRequest,
  CreateCheckoutSessionResponse,
  GetCheckoutSessionRequest,
  GetCheckoutSessionResponse,
  CreateRefundRequest,
  CreateRefundResponse,
  GetRefundRequest,
  GetRefundResponse,
  WechatPaymentCallbackRequest,
  WechatPaymentCallbackResponse,
  AlipayPaymentCallbackRequest,
  AlipayPaymentCallbackResponse,
  ListReceivablePeriodsRequest,
  ListReceivablePeriodsResponse,
  CreateOfflinePaymentRequest,
  CreateOfflinePaymentResponse,
  UpdateExternalInvoiceRequest,
  UpdateExternalInvoiceResponse,
} from '../models/operations/commerce.js'

/**
 * Get customer wallet
 *
 * Get a customer's Wallet view, including all credit buckets and recent
 * transactions.
 *
 * GET /customers/{customerId}/wallet
 */
export function getCustomerWallet(
  client: Client,
  req: GetCustomerWalletRequest,
  options?: RequestOptions,
): Promise<Result<GetCustomerWalletResponse>> {
  return request(() => {
    const pathParamsInput = {
      customerId: req.customerId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.getCustomerWalletPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.getCustomerWalletPathParamsWire, pathParams)
    }
    const path = `customers/${(() => {
      if (pathParams.customerId === undefined) {
        throw new Error('missing path parameter: customerId')
      }
      return encodeURIComponent(String(pathParams.customerId))
    })()}/wallet`
    return http(client)
      .get(path, options)
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.getCustomerWalletResponseWire, data)
        }
        return fromWire(data, schemas.getCustomerWalletResponse)
      })
  })
}

/**
 * List recharge products
 *
 * List all active recharge products available for purchase.
 *
 * GET /recharge-products
 */
export function listRechargeProducts(
  client: Client,
  req: ListRechargeProductsRequest = {},
  options?: RequestOptions,
): Promise<Result<ListRechargeProductsResponse>> {
  return request(() => {
    const query = toWire(
      {
        currency: req.currency,
      },
      schemas.listRechargeProductsQueryParams,
    )
    if (client._options.validate) {
      assertValid(schemas.listRechargeProductsQueryParamsWire, query)
    }
    const searchParams = toURLSearchParams(query)
    return http(client)
      .get('recharge-products', { ...options, searchParams })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.listRechargeProductsResponseWire, data)
        }
        return fromWire(data, schemas.listRechargeProductsResponse)
      })
  })
}

/**
 * Create order
 *
 * Create a new order (plan purchase, subscription renewal, or wallet top-up).
 *
 * Returns HTTP 201 on first creation. Replaying the same idempotency key returns
 * the stored order with HTTP 200.
 *
 * POST /orders
 */
export function createOrder(
  client: Client,
  req: CreateOrderRequest,
  options?: RequestOptions,
): Promise<Result<CreateOrderResponse>> {
  return request(() => {
    const body = toWire(req, schemas.createOrderBody)
    if (client._options.validate) {
      assertValid(schemas.createOrderBodyWire, body)
    }
    return http(client)
      .post('orders', { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.createOrderResponseWire, data)
        }
        return fromWire(data, schemas.createOrderResponse)
      })
  })
}

/**
 * Get order
 *
 * Retrieve an order by its ID.
 *
 * GET /orders/{orderId}
 */
export function getOrder(
  client: Client,
  req: GetOrderRequest,
  options?: RequestOptions,
): Promise<Result<GetOrderResponse>> {
  return request(() => {
    const pathParamsInput = {
      orderId: req.orderId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.getOrderPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.getOrderPathParamsWire, pathParams)
    }
    const path = `orders/${(() => {
      if (pathParams.orderId === undefined) {
        throw new Error('missing path parameter: orderId')
      }
      return encodeURIComponent(String(pathParams.orderId))
    })()}`
    return http(client)
      .get(path, options)
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.getOrderResponseWire, data)
        }
        return fromWire(data, schemas.getOrderResponse)
      })
  })
}

/**
 * Create checkout session
 *
 * Create a checkout session for an order, initiating a payment attempt with the
 * specified provider.
 *
 * POST /orders/{orderId}/checkout-sessions
 */
export function createCheckoutSession(
  client: Client,
  req: CreateCheckoutSessionRequest,
  options?: RequestOptions,
): Promise<Result<CreateCheckoutSessionResponse>> {
  return request(() => {
    const pathParamsInput = {
      orderId: req.orderId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.createCheckoutSessionPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.createCheckoutSessionPathParamsWire, pathParams)
    }
    const path = `orders/${(() => {
      if (pathParams.orderId === undefined) {
        throw new Error('missing path parameter: orderId')
      }
      return encodeURIComponent(String(pathParams.orderId))
    })()}/checkout-sessions`
    const body = toWire(req.body, schemas.createCheckoutSessionBody)
    if (client._options.validate) {
      assertValid(schemas.createCheckoutSessionBodyWire, body)
    }
    return http(client)
      .post(path, { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.createCheckoutSessionResponseWire, data)
        }
        return fromWire(data, schemas.createCheckoutSessionResponse)
      })
  })
}

/**
 * Get checkout session
 *
 * Retrieve a checkout session by its ID (for polling payment status after QR code
 * expiry or page reload).
 *
 * GET /checkout-sessions/{sessionId}
 */
export function getCheckoutSession(
  client: Client,
  req: GetCheckoutSessionRequest,
  options?: RequestOptions,
): Promise<Result<GetCheckoutSessionResponse>> {
  return request(() => {
    const pathParamsInput = {
      sessionId: req.sessionId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.getCheckoutSessionPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.getCheckoutSessionPathParamsWire, pathParams)
    }
    const path = `checkout-sessions/${(() => {
      if (pathParams.sessionId === undefined) {
        throw new Error('missing path parameter: sessionId')
      }
      return encodeURIComponent(String(pathParams.sessionId))
    })()}`
    return http(client)
      .get(path, options)
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.getCheckoutSessionResponseWire, data)
        }
        return fromWire(data, schemas.getCheckoutSessionResponse)
      })
  })
}

/**
 * Create refund
 *
 * Create a refund request for an order.
 *
 * POST /refunds
 */
export function createRefund(
  client: Client,
  req: CreateRefundRequest,
  options?: RequestOptions,
): Promise<Result<CreateRefundResponse>> {
  return request(() => {
    const body = toWire(req, schemas.createRefundBody)
    if (client._options.validate) {
      assertValid(schemas.createRefundBodyWire, body)
    }
    return http(client)
      .post('refunds', { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.createRefundResponseWire, data)
        }
        return fromWire(data, schemas.createRefundResponse)
      })
  })
}

/**
 * Get refund
 *
 * Retrieve a refund by its ID.
 *
 * GET /refunds/{refundId}
 */
export function getRefund(
  client: Client,
  req: GetRefundRequest,
  options?: RequestOptions,
): Promise<Result<GetRefundResponse>> {
  return request(() => {
    const pathParamsInput = {
      refundId: req.refundId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.getRefundPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.getRefundPathParamsWire, pathParams)
    }
    const path = `refunds/${(() => {
      if (pathParams.refundId === undefined) {
        throw new Error('missing path parameter: refundId')
      }
      return encodeURIComponent(String(pathParams.refundId))
    })()}`
    return http(client)
      .get(path, options)
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.getRefundResponseWire, data)
        }
        return fromWire(data, schemas.getRefundResponse)
      })
  })
}

/**
 * WeChat Pay callback
 *
 * WeChat Pay payment callback. OpenMeter verifies the signature, confirms the
 * payment fact, and fulfills the order.
 *
 * POST /payment-providers/wechat/callback
 */
export function wechatPaymentCallback(
  client: Client,
  req: WechatPaymentCallbackRequest,
  options?: RequestOptions,
): Promise<Result<WechatPaymentCallbackResponse>> {
  return request(() => {
    const body = toWire(req, schemas.wechatPaymentCallbackBody)
    if (client._options.validate) {
      assertValid(schemas.wechatPaymentCallbackBodyWire, body)
    }
    return http(client)
      .post('payment-providers/wechat/callback', { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.wechatPaymentCallbackResponseWire, data)
        }
        return fromWire(data, schemas.wechatPaymentCallbackResponse)
      })
  })
}

/**
 * Alipay callback
 *
 * Alipay payment callback. OpenMeter verifies the signature, confirms the payment
 * fact, and fulfills the order.
 *
 * POST /payment-providers/alipay/callback
 */
export function alipayPaymentCallback(
  client: Client,
  req: AlipayPaymentCallbackRequest,
  options?: RequestOptions,
): Promise<Result<AlipayPaymentCallbackResponse>> {
  return request(() => {
    const body = toWire(req, schemas.alipayPaymentCallbackBody)
    if (client._options.validate) {
      assertValid(schemas.alipayPaymentCallbackBodyWire, body)
    }
    return http(client)
      .post('payment-providers/alipay/callback', { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.alipayPaymentCallbackResponseWire, data)
        }
        return fromWire(data, schemas.alipayPaymentCallbackResponse)
      })
  })
}

/**
 * List receivable periods
 *
 * List receivable periods for a customer.
 *
 * GET /customers/{customerId}/receivable-periods
 */
export function listReceivablePeriods(
  client: Client,
  req: ListReceivablePeriodsRequest,
  options?: RequestOptions,
): Promise<Result<ListReceivablePeriodsResponse>> {
  return request(() => {
    const pathParamsInput = {
      customerId: req.customerId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.listReceivablePeriodsPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.listReceivablePeriodsPathParamsWire, pathParams)
    }
    const path = `customers/${(() => {
      if (pathParams.customerId === undefined) {
        throw new Error('missing path parameter: customerId')
      }
      return encodeURIComponent(String(pathParams.customerId))
    })()}/receivable-periods`
    const query = toWire(
      {
        page: req.page,
      },
      schemas.listReceivablePeriodsQueryParams,
    )
    if (client._options.validate) {
      assertValid(schemas.listReceivablePeriodsQueryParamsWire, query)
    }
    const searchParams = toURLSearchParams(query)
    return http(client)
      .get(path, { ...options, searchParams })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.listReceivablePeriodsResponseWire, data)
        }
        return fromWire(data, schemas.listReceivablePeriodsResponse)
      })
  })
}

/**
 * Create offline payment
 *
 * Record an offline payment (bank transfer, enterprise remittance) for a customer.
 * The payment is held for reconciliation before being applied to a receivable
 * period.
 *
 * POST /customers/{customerId}/offline-payments
 */
export function createOfflinePayment(
  client: Client,
  req: CreateOfflinePaymentRequest,
  options?: RequestOptions,
): Promise<Result<CreateOfflinePaymentResponse>> {
  return request(() => {
    const pathParamsInput = {
      customerId: req.customerId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.createOfflinePaymentPathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.createOfflinePaymentPathParamsWire, pathParams)
    }
    const path = `customers/${(() => {
      if (pathParams.customerId === undefined) {
        throw new Error('missing path parameter: customerId')
      }
      return encodeURIComponent(String(pathParams.customerId))
    })()}/offline-payments`
    const body = toWire(req.body, schemas.createOfflinePaymentBody)
    if (client._options.validate) {
      assertValid(schemas.createOfflinePaymentBodyWire, body)
    }
    return http(client)
      .post(path, { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.createOfflinePaymentResponseWire, data)
        }
        return fromWire(data, schemas.createOfflinePaymentResponse)
      })
  })
}

/**
 * Update external invoice
 *
 * Attach or update an external invoice reference on a receivable period.
 *
 * PUT /customers/{customerId}/receivable-periods/{periodId}/external-invoice
 */
export function updateExternalInvoice(
  client: Client,
  req: UpdateExternalInvoiceRequest,
  options?: RequestOptions,
): Promise<Result<UpdateExternalInvoiceResponse>> {
  return request(() => {
    const pathParamsInput = {
      customerId: req.customerId,
      periodId: req.periodId,
    }
    const pathParams = client._options.validate
      ? toPathWire(pathParamsInput, schemas.updateExternalInvoicePathParams)
      : pathParamsInput
    if (client._options.validate) {
      assertValid(schemas.updateExternalInvoicePathParamsWire, pathParams)
    }
    const path = `customers/${(() => {
      if (pathParams.customerId === undefined) {
        throw new Error('missing path parameter: customerId')
      }
      return encodeURIComponent(String(pathParams.customerId))
    })()}/receivable-periods/${(() => {
      if (pathParams.periodId === undefined) {
        throw new Error('missing path parameter: periodId')
      }
      return encodeURIComponent(String(pathParams.periodId))
    })()}/external-invoice`
    const body = toWire(req.body, schemas.updateExternalInvoiceBody)
    if (client._options.validate) {
      assertValid(schemas.updateExternalInvoiceBodyWire, body)
    }
    return http(client)
      .put(path, { ...options, json: body })
      .json()
      .then((data) => {
        if (client._options.validate) {
          assertValid(schemas.updateExternalInvoiceResponseWire, data)
        }
        return fromWire(data, schemas.updateExternalInvoiceResponse)
      })
  })
}
