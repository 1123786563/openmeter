# Final Whole-Branch Adversarial Review Brief — Issue #5

You are the final adversarial reviewer for the whole issue-#5 branch, using the strongest available reasoning. Your job: find anything the task-level reviews missed — cross-commit interactions, regressions, security, i18n integrity, evidence authenticity. Assume everything until now could be wrong.

Worktree: /Users/wuyongjun/trea/openmeter-issue-5 (branch codex/admin-config-05, base 45f89e50d = main)
Review range: 45f89e50d..HEAD (plan commit c4119ca32 + implementation commit a6ff556ef `feat(admin): 计划发布/归档/克隆新版本`).
Context: /Users/wuyongjun/trea/openmeter-issue-5/.superpowers/sdd/issue-5-plan-lifecycle/progress.md (ledger), spec-review-report.md, quality-review-report.md, browser-walkthrough-report.md (read them all — audit, don't trust).
Authoritative spec: `gh issue view 5 --repo 1123786563/openmeter --comments` (prescription comment; read-only).

Angles (each with concrete evidence):
1. i18n deep-compare: programmatic key-set equality zh-CN vs en for the whole config.plans subtree after this change (actions/publishConfirm/archiveConfirm/cloneConfirm/toast added in both); every new t() ref in the diff resolves in BOTH locales; no duplicate keys; no dead keys introduced (e.g. keys added but never referenced).
2. Button visibility rules vs spec (the single source of truth): draft→发布 only; active→归档+克隆; scheduled/archived→克隆 only; ALL hidden while any of the three mutations isPending (statusBusy). Check the exact JSX conditions — is there a state where TWO wrong buttons show or the clone button wrongly shows on draft? plan.status typing ('draft'|'active'|'archived'|'scheduled') makes `!== 'draft'` exhaustive-correct — verify no status slips through unhandled.
3. Mutation wiring: publish/archive use api.plans.publish/archive with {planId}; clone uses clonePlanNextVersion with plan.id (not plan.key — spec says id or key allowed, id chosen; confirm the v1 endpoint receives the LATEST PUBLISHED version id — clicking 克隆 from an archived v1 while v3 active exists: does the spec/comment anticipate this? The comment says backend 4xx passthrough, no frontend pre-check — confirm implementation does NOT pre-check).
4. Invalidation semantics: trio nsPrefix('plans')+nsPrefix('plans-page')+queryKeys.plan(id) — trace which UI surfaces consume each key (plans list page, subscription wizard usePlans, plan detail). After publish: does the DETAIL page refetch (queryKeys.plan(plan.id) invalidated — same id, correct)? After clone: navigate to NEW id — fresh query fires (invalidation of old id harmless). Any consumer of plans data that would show STALE data after these mutations (e.g. create-subscription wizard opened later — uses 'plans' key, invalidated, refetches on mount — OK)? Verify by reading the actual cache consumers.
5. Error paths: handleServerError on all three onError — dialog stays open on error (confirming not reset); is dialog-close-then-reopen safe (state machine 'publish'|'archive'|'clone'|null)? Double-submit protection: ConfirmDialog isLoading + statusBusy hide — can a second POST fire while first in flight (dialog confirm button disabled via isLoading — verify ConfirmDialog implementation actually disables).
6. LegacyPlan type honesty: v1 Plan camelCase subset (api/openopenapi v1 spec) — compare field-for-field with api/openapi.yaml v1 Plan schema (id/name/key/version/currency/billingCadence/status/createdAt/updatedAt); no drift.
7. Regressions: diff file list is exactly the 5 planned files; plan-detail.tsx existing rendering (phases/rate cards table, InfoRows) untouched; plans list page untouched; routeTree.gen.ts NOT in diff (no route changes — verify none needed); existing smoke e2e untouched.
8. Cross-commit: plan commit documents deviations; implementation commit message matches issue prescription exactly (`feat(admin): 计划发布/归档/克隆新版本`).
9. Security: no secrets, no new deps, no eval/dynamic imports; toast content from server error bodies is React-escaped (sonner renders text, not HTML — verify).
10. Evidence audit: rerun spot checks yourself — `cd web && pnpm build && pnpm lint` exit codes; verify walkthrough report's POST endpoint records and screenshots exist (/tmp/issue5-shots/*, /tmp/issue5-walkthrough-results.json) and correspond to this HEAD; ledger progress.md rulings consistent with the three reports.
11. AGENTS.md conventions for new code (no any/ts-ignore in added lines, comment intent not narration, no trivial-helper extraction violations).

Verdict: PASS (safe to present) or FAIL, with Critical/Important/Minor findings + file:line evidence. Explicitly state which angles you ran and which you skipped (with reason). No fixes — report only. No GitHub writes. No subagents. Write your report to /Users/wuyongjun/trea/openmeter-issue-5/.superpowers/sdd/issue-5-plan-lifecycle/final-review-report.md ending with `Ruling: PASS|FAIL`.
