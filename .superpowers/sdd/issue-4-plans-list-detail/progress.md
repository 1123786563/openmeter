# SDD ledger — plan: docs/superpowers/plans/issue-4-plans-list-detail.md

- Issue: https://github.com/1123786563/openmeter/issues/4 `[admin-config 04/29] 计划：列表与详情（只读）`
- Worktree: /Users/wuyongjun/trea/openmeter-issue-4 (branch codex/admin-config-04, base 18309b955 = main = origin/main，含 #1–#3 成果)
- Plan commit: 96e1111c0 (docs(admin): issue #4 计划列表与详情实施计划)
- Selection rationale: 最小编号 open Issue（#4–#29 全部 ready-for-agent）；唯一依赖 #1 已满足（merged ea31ef45a、issue closed 2026-08-27T01:15:45Z）；处方化评论计划（每步完整代码）；非 PR/重复/阻塞/讨论项。#4 是计划域只读列表+详情，与 #5（写操作）正交切分（总纲 Task 3 前半）。
- Controller environment: 临时 POSIX tool-bash 块再次加入 router-standard preset（本会话 controller 有直接 bash）；任务结束必须移除。
- Controller prettier 调查（pre-final-review）：`pnpm exec prettier --check src/api/hooks.ts` 在 HEAD 与**基线内容放回真实路径**两种情况均告警，唯一分歧 = line 485 recharge hunk（多行 vs 折叠）——与 #2/#3 台账记录的预存违规一致，100% 预存于 base 18309b955，非本任务引入。issue-4 新增行（hooks.ts 170-199）在 prettier 输出 diff 中零分歧（格式干净）。附带发现（工具奇观，已实证）：同字节内容经 `./__probeN__.ts` 探针路径检查 PASS、经 `src/api/hooks.ts` 真实路径 FAIL——prettier 插件链（sort-imports/tailwind）对路径敏感的状态行为；基线对照实验排除了内容因素。探针后已恢复原文件（md5 9d3122813... 一致，树净）。
- Approval state: NO external side effects for issue #4 without separate user approval (push/merge/close stay local).
- Controller pre-verified SDK/前端契约（写入 plan 文档「SDK 契约核实记录」节）：`api.plans.list/get` 签名与响应形状、`StringFieldFilterExact` 接受纯字符串、sort 形状与兄弟页一致、Plan/PlanPhase/RateCard/Price 逐字段与评论一致、EnumBadge/StatusBadge/formatAmount/ServerTable/i18n 现状全部核实。已知偏差风险 3 条写入 plan。

## Task 1 — plans list page + detail page + routes + i18n (issue comment steps 1-7)

- Status: CLOSED — local completion declared at 45f89e50d (spec review PASS + quality review PASS + browser walkthrough PASS + final review SAFE TO PRESENT; no fix rounds needed)
- Implementer: subagent (1 dispatch; no subagents spawned, no GitHub writes, tree clean after commit; only .superpowers/sdd/ untracked per convention)
- Implementation evidence: pnpm build exit 0 (/tmp/issue4-build.log, vite 285ms), pnpm lint exit 0 (/tmp/issue4-lint.log), pnpm test:e2e exit 0 — 2 passed (/tmp/issue4-e2e.log, ports :4173/:9999 cleaned pre-run); env: SDK dist rebuilt + re-injected into node_modules, pnpm-lock.yaml untouched. 10 files +547/−9 incl. routeTree.gen.ts ($planId node only).
- i18n programmatic compare: config.plans 28 keys + top-level plan 10 keys, zh/en trees identical, no dup keys, interpolation vars ({{index}}/{{duration}}/{{count}}) consistent per key.
- Deviations recorded by implementer: (1) PlansPage status state typed `useState<PlanListParams['status']>()` + cast — comment's `useState<string | undefined>` hit TS2322 vs its own narrow union; pattern byte-matches orders.tsx sibling. (2) plan-detail.tsx dropped unused `Plan` type import (TS6196 noUnusedLocals), kept RateCard. (3) prettier/formatting-only adaptations (new files, i18n indent, one en.ts line fold). Route param pattern from #3 NOT needed — comment's useParams({from}) + component: PlanDetail passed eslint/TS as-is.
- Spec review (subagent 12735b0c): PASS — 0 Critical / 0 Important / 2 Minor (report: spec-review-report.md). 10/10 checklist PASS with evidence: file list exact (8 comment files + routeTree $planId-only), hooks/query-keys/status-badge/list-7-cols/detail-2-level/5-branch summary/3 fallbacks/routes/i18n all verified against comment + SDK dist types. Both deviations independently REPRODUCED by reviewer (/tmp tsc program with real tsconfig.app.json + real @/api/hooks): TS2322 for comment's own self-contradictory status state typing (fix byte-matches orders.tsx:62/197-204, pre-branch since 925f6be4d), TS6133 for unused Plan import (report said TS6196 — same class, doc-level notation difference = Minor-1). Recharge hunk pre-existence git-blame-confirmed at 925f6be4d. Reviewer own runs: pnpm lint exit 0, full-program tsc exit 0 (routeTree pulls all routes/pages), i18n script re-run PASS×4 (28/10/516 keys). Evidence timeline coherent: 13:13 baseline build → 13:16 i18n → 13:17 three logs → 13:18:04 commit. Minor-2: first failed build output not persisted (both errors independently reproduced so narrative stands; future tasks should tee first failures).
- Quality review (subagent 650789dd): PASS — 0 Critical / 0 Important / 3 Minor (report: quality-review-report.md). Reviewer's own runs: pnpm lint exit 0, pnpm build exit 0 (post-build git status clean = routeTree byte-sync); programmatic i18n (config.plans 28 + plan 10 keys zh/en trees+interpolation identical, 516/516 whole-file no dup siblings, 37 t() refs all resolve, 38 new keys zero dead — script /tmp/issue4-quality-i18n-check.mjs); disabled-pattern scan zero in handwritten code; diff --stat exactly 10 files +547/-9. Minors: M1 phase.key??index right-branch unreachable per SDK types (issue-prescribed defensive, kept); M2 hooks.ts recharge hunk prettier pre-existing at base (reviewer's same-condition recheck CONFIRMS controller's earlier ruling; eslint has no format gate — future chore); M3 route wiring uses meter-detail precedent (component+useParams from) vs features' props+eslint-disable wrapper — both patterns coexist, chosen one cleaner. Reviewer observed a transient hooks.ts working-tree revert mid-review (actually the controller's base-comparison experiment at 13:24-13:26, md5-verified restored); reviewer independently restored from HEAD too; controller verified final state: md5 9d3122813... = HEAD, diff 45f89e50d = 0, tree clean. No fix round required (no Critical/Important).
- Browser walkthrough (subagent abff0a82, at 45f89e50d): PASS — 86/86 assertions, pageerror 0 across all 7 tests (explicitly asserted), 26 plans GETs captured; scenarios a–h all PASS (report: browser-walkthrough-report.md; raw /tmp/issue4-walkthrough-results.json; traces /tmp/issue4/test-results/*/trace.zip ×7; 9 shots /tmp/issue4-shots/ + Pillow pixel-check.json). Wire evidence matches controller-verified facts exactly (snake_case mock bodies, filter[status]=/page[number]=/sort=created_at desc, /api/v3/openmeter/plans*). Highlights: scenario b same-request filter+page-reset proof; scenario d 2 phases × 5 price types full coverage incl. cadence/currency fallbacks; scenario e skeleton + 404 fallbacks; scenario f full en locale switch; scenario g unknown price.type renders EnumBadge fallback without crash, empty phases + 300-char names fine. First run failed at login for all 7 (agent's own infra: preview server died mid-run, no webServer in agent's pw.config) — agent diagnosed, restarted servers, and re-ran to full green (run 2 of record; run-1 failure was test-harness infra, NOT product). Rebuild note: pre-existing dist had no VITE_CASDOOR_* baked (one stale env chunk residue) — agent rebuilt gitignored dist with repo e2e webServer env before testing; source tree untouched, git-verified. Minors (pre-existing shared-component behavior, non-blocking): M1 pagination copy 'Page x of y'/'Rows per page' hardcoded English in zh UI (components/data-table/pagination.tsx); M2 zh UI dates use date-fns enUS locale (lib/format.ts) — same family as issue-3 walkthrough finding; M3 env note for future handoffs (rebuild dist with e2e env or note in delivery docs). Visual channel: read_image/describe_image confirmed broken (same as #1–#3); programmatic evidence = DOM assertions + traces + Pillow metrics; human eyeball of /tmp/issue4-shots/ recommended.
- Final whole-branch adversarial review (subagent e54c2292, range 18309b955..45f89e50d = 2 commits, 11 files +608/−9): SAFE TO PRESENT — 0 Critical / 0 Important / 5 Minor (report: final-review-report.md). All 9 angles PASS: spec fidelity (both acceptance criteria proven by code + wire; all 4 deviations independently re-adjudicated just, deviation-1 byte-matches orders.tsx:62/199-203); SDK contract independently re-derived from dist/ (three-way agreement: derive ↔ implementation ↔ observed wire); query semantics (ns() namespace prefix + structured-hash key stability, enabled gate, same-request filter+page-reset wire proof, 404 retry 5×2 matches walkthrough's 11 GETs exactly, three keys zero cross-contamination); i18n AST deep-check (516/516 trees, 33 literal t() + 9 dynamic keys both-locale resolvable, 38 new keys zero dead — script /tmp/issue4-final-i18n-check.mjs); regression surface (route-ID set diff only adds $planId, sidebar/layout/deps/lockfile zero diff, post-build tree clean); cross-commit consistency; security clean; evidence audit (13:13→13:41 timeline monotonic, recharge-hunk pre-existence FOURTH independent confirmation via git blame→925f6be4d); conventions OK. Minors: ①recharge hunk prettier pre-existing; ②phase.key??index dead branch per types (prescribed defensive); ③TS6196/TS6133 notation difference; ④zh-UI English pagination copy + enUS dates (shared-component pre-existing); ⑤placeholder-page JSDoc example stale (comment-level). Reviewer own runs: pnpm lint exit 0, i18n AST ALL PASSED, pnpm build exit 0 + tree clean.
- Controller final verification trio at HEAD 45f89e50d (post-final-review): pnpm build exit 0 (✓ 368ms, /tmp/issue4-final-build.log), pnpm lint exit 0 (/tmp/issue4-final-lint.log), pnpm test:e2e exit 0 — 2 passed 6.4s, ports cleaned pre-run (/tmp/issue4-final-e2e.log). Tree clean (only ?? .superpowers/sdd/).
- Ruling: Task 1 CLOSED — local completion declared at 45f89e50d: implementation + coverage tests (build/lint/e2e exit 0 at final HEAD, controller-verified) + spec review PASS + quality review PASS (no fix round needed — 0 Critical/Important throughout) + browser walkthrough PASS (86/86, 0 pageerror) + final whole-branch review SAFE TO PRESENT. Issue #4 acceptance criteria both satisfied: 列表可筛选状态并分页 (wire-proven filter[status]+page[number], page reset on filter change, 3-page pagination) and 详情正确渲染所有阶段与价目卡结构 (2 phases × 5 price types rendered with badges/fallbacks, walkthrough-verified).
- Remaining risks (for report): (1) visual evidence programmatic (DOM+trace+Pillow; deployment lacks image input, same as #1–#3) — human eyeball of /tmp/issue4-shots/ recommended; (2) pre-existing shared-component i18n gaps surfaced: zh pagination copy 'Page x of y'/'Rows per page' English + date-fns enUS dates in zh UI — future hygiene issue; (3) hooks.ts recharge hunk prettier violation pre-existing (four independent confirmations) — future chore commit; (4) worktree pre-existing dist lacks VITE_CASDOOR_* bake (one stale env chunk residue) — future handoffs must rebuild dist with e2e env before preview walkthrough; (5) placeholder-page JSDoc example now stale (comment-level); (6) externalization (push/merge/close #4) NOT approved — awaiting user decision.
- Delivery gate: delivery_check PASS — 27 evidence items accepted (10 files: plan + 3 implementation files + ledger + 5 review/walkthrough reports; 4 runs: implementer trio + controller final trio; 2 tests with script/JSON targets: i18n four-round + walkthrough; 1 text visual-channel disposition item; 1 external git-chain file; 10 single-scalar numerics). First gate run FAILED honestly (unreviewed image items + composite numeric result + external target-as-command) — fixed per issue-#3 precedent: visual channels probed broken this session (read_image: model glm-5.3 no image input; describe_image: baseURL misconfigured — both probed directly by controller), images withdrawn as gate evidence with disposition documented; numerics split to single scalars; external target pointed at real output file /tmp/issue4-git-chain.txt. requireSmoke:false (code+ledger deliverable; in-browser verification of the built artifact in walkthrough record). Gate satisfied on re-run. Session objective (issue #4 local completion + report) achieved.

## Externalization (user-approved 2026-08-27 ~15:23+08:00, executed by follow-up wake session)

- User approved all three external side effects via explicit question (option: 批准全部三项).
- Executed in order, each verified by this session:
  1. `git push origin codex/admin-config-04` → remote ref = 45f89e50da34 (ls-remote verified).
  2. Local main ff-merge 18309b955 → 45f89e50d (`git merge --ff-only`), `git push origin main` → remote main = 45f89e50da34 (ls-remote verified). Pre-push safety: remote main == local main == 18309b955 (no concurrent push), main ancestor of branch, tree clean.
  3. `gh issue close 4 --comment <completion summary>` → CLOSED at 2026-08-27T07:24:06Z (gh issue view verified). Comment covers changes, acceptance mapping, verification runs, review outcomes, adjudicated deviations.
- Issue #4 fully terminal: implemented → reviewed → merged (ff) → pushed → closed. Worktree openmeter-issue-4 + branch retained per standing-task rule (保留隔离分支和提交). Coordination note in main checkout superseded.
